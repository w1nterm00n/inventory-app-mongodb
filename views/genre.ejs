<!DOCTYPE html>
<html>
    <head>
        <link rel="stylesheet" href="/../style.css">
    </head>
<body>

    <div class="genre page">
        <%- include('navbar') %>

        <div class="content">
            <h1 style="text-align: center;">Genre</h1>

            <!-- <% if (albums[0]) { %>
                <h2 style="margin-bottom: 60px;"> <%= albums[0].genre_name %> </h2>
           <% } else { %>
                <h2 style="margin-bottom: 60px;"> Seems like there's no albums </h2>
           <% } %>

            <table  class="allAlbumsTable">
                <tbody>
                    <% albums.forEach(function(album) { %>
                        <tr>
                           <% if (album.img_url) { %>
                                <td><img class="allAlbums_image" src="<%= album.img_url %>" alt="Album Cover"></td>
                           <% } else { %>
                                <td><p>No Cover Available</p></td>
                           <% } %>
                          <td> <a href="/album/<%= album.id_album %>"> <%= album.name %> </a> </td>
                          <td> <%= album.artist %> </td>
                          <td> <%= album.genre_name %> </td>
                          <td> <%= album.price %> </td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>

            <div class="buttons">
                <form action="/genre/update/<%= id %>?_method=GET" method="POST">
                    <button type="submit" class="btn delete-btn change-btn">Change</button>
                </form>

                <form action="/genre/<%= id %>?_method=DELETE" method="POST">
                    <button type="submit" class="btn delete-btn change-btn">Delete</button>
                </form>
            </div> -->
            
        </div>
    </div>

</body>
</html>